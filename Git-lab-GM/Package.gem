# Step 1: Authenticate
$ echo ":github: Bearer GH_TOKEN" >> ~/.gem/credentials

# Step 2: Build
$ gem build gmrightkenja-.gemspec

# Step 3: Publish
$ gem push --key github --host https://rubygems.pkg.github.com/gmright2 gmrightkenja--1.0.0.gem
